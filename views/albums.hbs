<div>
    <h1>albums</h1>
    {{#each items}}
    <h2>name: {{ name }}</h2>
    <a href="/tracks/{{id}}" id="{{id}}">View Tracks</a>
    {{#each images}}
    <p>image: <img src="{{url}}" alt="{{name}}"></p>
    {{/each}}
    {{/each}}
</div>